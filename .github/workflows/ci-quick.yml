name: CI (quick)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quick:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Quick checks (JVM tests + Android assemble)
        run: |
          ./gradlew --no-daemon :platform:desktop-app:build
          ./gradlew --no-daemon jvmTest
          ./gradlew --no-daemon :platform:android-app:assembleDebug

